# CLAUDE.md - Agent Operation Boundaries & Project Context

## 项目概述

**项目名称**: Linux 运维智能巡检与日志诊断助手  
**所属课程**: 《Linux 系统管理与 Shell 编程》  
**开发目录**: D:/linux开发/  
**项目目标**: 开发一个命令行巡检与日志诊断工具，将系统状态采集、日志异常提取、诊断建议和报告生成串联起来。

---

## 代理操作边界（Agent Boundaries）

### 禁止操作（严禁执行）

1. **禁止删除任何用户文件**  
   - 不得执行 `rm`、`rm -rf`、`del`、`git rm` 等删除命令  
   - 不得通过任何方式删除、移除用户的源代码、文档、配置、数据文件  
   - 如需清理临时文件或中间产物，必须先向用户说明原因并等待明确授权  
   - 禁止执行 `git reset --hard`、`git clean -f`、`git checkout -- .` 等破坏性 Git 操作

2. **禁止修改受保护文件**  
   - 以下文件未经用户明确许可不得修改：  
     - `CLAUDE.md`（本文件）  
     - `课程项目开发规范.pdf`  
     - `课程项目开发规范补充说明（AI时代工程实践要求）.pdf`  
     - `Linux运维智能巡检与日志诊断助手_项目规划书.docx`  
     - `Linux运维智能巡检与日志诊断助手_开题汇报PPT_AI工程实践改进版.pptx`  
   - 不得修改 `.git` 目录中的任何内容（除通过 git 命令正常操作外）

3. **禁止跳过Git Hooks**  
   - 不得使用 `--no-verify`、`--no-gpg-sign` 等跳过检查的参数  
   - 不得使用 `--force` 推送到 main/master 分支

4. **禁止在未授权情况下推送到远程仓库**  
   - 每次 `git push` 操作前必须获得用户明确授权  
   - 不得强制推送（`git push --force`）

5. **禁止修改Git配置**  
   - 不得修改 `.git/config` 文件  
   - 不得执行 `git config` 修改命令

### 受限操作（需用户授权）

6. **创建新目录/文件**  
   - 在项目根目录下创建新目录或文件前，应说明用途  
   - 小型代码文件（脚本、模块）可在合理情况下直接创建

7. **安装软件包/依赖**  
   - 安装系统级或Python包前需用户授权  
   - 使用 `pip install`、`apt-get` 等包管理器前需说明原因

8. **修改已有代码**  
   - 可以修改项目中已存在的代码文件  
   - 大规模重构需先说明并在获得授权后执行  
   - 每次修改后需验证代码完整性

9. **Git Commit 操作**  
   - 每次 commit 前应展示变更内容摘要  
   - commit message 应清晰说明修改内容  
   - 不得在用户未确认的情况下提交

### 必须遵守的规则

10. **保持完整的Git提交记录**  
    - 每个功能版本独立提交，禁止一次性提交全部代码  
    - 每次提交应体现功能增加、Bug修复、测试修改、文档更新等类别

11. **版本迭代开发**  
    - 严格按照版本规划进行迭代开发  
    - 每完成一个版本功能后必须进行测试验证  
    - 版本号格式：v<major>.<minor>（如 v0.1, v0.2, v1.0）

12. **代码注释与文档**  
    - 关键功能必须有注释说明  
    - 对外接口（脚本参数、返回值）需说明用法  
    - 复杂逻辑需简要说明实现思路

13. **测试要求**  
    - 每个版本完成后需进行功能测试  
    - 关键模块需有测试用例  
    - 记录测试结果和发现的问题

---

## 项目架构规划

```
D:/linux开发/
├── ops-assist              # 主入口脚本（bash）
├── modules/
│   ├── check_system.sh     # 系统巡检模块
│   └── log_analyzer.sh     # 日志分析模块
├── scripts/
│   ├── report.py           # 报告生成模块（Python）
│   └── diagnose.py         # 诊断建议模块（Python）
├── config/
│   └── ops-assist.conf     # 配置文件
├── samples/
│   └── sample_logs/        # 样例日志
├── reports/                # 巡检报告输出目录
├── logs/                   # 工具自身日志
└── tests/                  # 测试脚本和测试数据
```

---

## 使用技术清单

本项目必须至少使用以下技术中的 **4项以上**：

| 序号 | 技术 | 本项目使用 |
|------|------|------------|
| 1 | Shell脚本（bash） | 主入口、巡检模块、日志分析模块 |
| 2 | Linux系统命令 | df、free、ps、ss、systemctl、journalctl 等 |
| 3 | grep/sed/awk 文本处理 | 日志过滤、字段提取、异常统计 |
| 4 | 管道与重定向 | 命令组合、结果保存 |
| 5 | 文件与权限管理 | 目录创建、日志读取权限 |
| 6 | crontab 定时任务 | 无人值守巡检 |
| 7 | Python 调用 Linux 功能 | 报告生成、诊断建议 |
| 8 | Git 代码管理 | 版本控制、提交记录 |

---

## 版本迭代计划

| 版本 | 完成内容 | 测试重点 |
|------|----------|----------|
| v0.1 | 项目目录结构、主入口脚本雏形、参数解析 | 命令参数是否正确分发 |
| v0.2 | 系统状态巡检模块 | 磁盘、内存、服务状态数据是否正确 |
| v0.3 | 日志分析模块 | 日志关键词统计是否正确 |
| v0.4 | 诊断建议与报告生成 | 报告是否完整、诊断是否合理 |
| v0.5 | crontab定时巡检与集成 | 定时任务是否正常执行 |
| v1.0 | 整体联调、异常测试、Bug修复 | 异常场景处理、整体稳定性 |

---

## 技术参考

- 开发规范：`课程项目开发规范.pdf`（23页）
- 补充说明：`课程项目开发规范补充说明（AI时代工程实践要求）.pdf`（11页）
- 项目规划：`Linux运维智能巡检与日志诊断助手_项目规划书.docx`

## Git配置

- 仓库地址：待用户提供
- 分支策略：main 分支为稳定版本，每个版本打 tag
- 提交规范：`[模块] 简短描述` 或 `[版本] 简短描述`
